name: flutter_persistent_queue
description: A new Flutter package project.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.0.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  localstorage: ^1.2.0
  synchronized: ^2.0.2+1

dev_dependencies:
  flutter_test:
    sdk: flutter
