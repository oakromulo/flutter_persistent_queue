name: flutter_persistent_queue
description: Simple file-based non-volatile queue for flutter.
version: 0.0.1
author: Romulo Carvalho <oakromulo@gmail.com>
homepage: https://github.com/oakromulo/flutter_persistent_queue

environment:
  sdk: ">=2.0.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  localstorage: ^1.2.0
  synchronized: ^2.0.2+1

dev_dependencies:
  flutter_test:
    sdk: flutter
