name: flutter_persistent_queue
description: Simple file-based non-volatile queue for flutter. Ideal for in-device sequential buffers that persist between app runs.
version: 0.1.3+1
author: Romulo Carvalho <oakromulo@gmail.com>
homepage: https://github.com/oakromulo/flutter_persistent_queue
repository: https://github.com/oakromulo/flutter_persistent_queue.git
issue_tracker: https://github.com/oakromulo/flutter_persistent_queue/issues

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  localstorage: ^1.2.0
  meta: ^1.1.6
  synchronized: ^2.0.2+1
