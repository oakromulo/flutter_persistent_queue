name: test_app
description: "dummy app for integration tests: `flutter drive --target=test_driver/app.dart`"

environment:
  sdk: ">=2.2.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_persistent_queue:
    path: ../

dev_dependencies:
  flutter_driver:
    sdk: flutter
  test: any

flutter:
  uses-material-design: true
