name: test_app
description: dummy app for integration tests
version: 0.0.1+1

environment:
  flutter: ">=1.2.0 <2.0.0"
  sdk: ">=2.3.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_persistent_queue:
    path: ../

dev_dependencies:
  flutter_driver:
    sdk: flutter
  test: any

flutter:
  uses-material-design: true
